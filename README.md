Это карточная игра, вдохновленная Munchkin

🛠 Технологии
Zenject, UniRx, DoTween, UniTask

Инфраструктура запускается из BootInstaller (тут в основном асинхронные последовательные сервисы для игры и менюшка), геймплей из LobbyInstaller (тут уже запускаются игровые механики, преймущественно mvp based)

**Архитектура слоёв (геймплей/UI)**
- Core/DataLayer: доменные модели, данные и use case’ы. Не знает об UI.
- UIContracts: чистые интерфейсы экранов и DTO для UI (CardViewData и т.д.).
- Presenters/Effects: оркестрация геймплейного потока (GameplayFlowPresenter + FSM), маппинг домена → UI DTO, вызовы окон через `IUIService.ShowAsync<TContract>()`, подписки на сигналы UI.
- UI.Infrastructure: `IUIService` и фабрики экранов, резолвят контракт → конкретный `UIScreen` префаб.
- ViewsLayer: реализации экранов (`UIScreen`), которые имплементируют контракты и поднимают события/observable наружу; не знают домен/use case’ы.
- Направление зависимостей: UIContracts ← Presenters/Effects → Core; UIContracts ← Views; UIContracts ← UI.Infrastructure.

📂 Структура проекта
CardGame/  
├── Assets/  
│   ├── Scripts/  
│   │   ├── Infrastructure/  
│   │   ├── Gameplay/      
│   │   ├   ├──DataLayer/    
│   │   ├   ├──PresentersLayer/   
│   │   ├   ├──ViewsLayer/      
